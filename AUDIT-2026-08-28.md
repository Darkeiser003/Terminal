# Auditoría observable del banner y la rejilla

## Alcance

- Código revisado: `src-tauri/src/terminal/tabs.rs`, `src-tauri/src/platform/system_info.rs`, `src/components/TerminalPane.svelte` y `tests/e2e/smoke.mjs`.
- Sesión nativa observada con `src-tauri/target/debug/winslim-terminal.exe`.
- Log correlacionado: `%APPDATA%/winslim-terminal/logs/main.log`.

## Evidencia reproducida

1. En la sesión `c4hn7v` el log no registró errores, pero al pasar a dos paneles una casilla conservaba el banner completo y otra el compacto. La captura `winslim-audit-native-split.png` conserva ese estado.
2. En sesiones anteriores aparecía `ReferenceError: paneCountChanged is not defined` en `TerminalPane.svelte`; se corrigió y se añadió una comprobación de alcance al test frontend.
3. El arranque generaba el archivo de banner con el viewport persistido (por ejemplo, 48 columnas) antes de que el xterm midiera 99 columnas. Al refluirlo encima del prompt aparecían valores concatenados y separadores residuales.
4. El PTY y el repintado de resize emitían `pty-data` desde hilos distintos. Sin serialización, dos bloques podían llegar a xterm intercalados.

## Correcciones aplicadas

- `TabManager` serializa las emisiones de salida y el vaciado de la cola inicial.
- Las pestañas nuevas retienen el inicializador hasta el primer `pty_resize` real; justo antes se regeneran el archivo y el layout para ese tamaño.
- Un repintado pendiente que coincide con el viewport recién impreso se descarta al recibir el marcador de inicialización; así no se superpone una segunda cabecera.
- El smoke exige cabecera propia, modo homogéneo, ausencia de líneas mezcladas y ausencia de prompt antes del último campo del banner.

## Capturas

- Antes de la corrección de viewport: `C:\Users\Administrador\AppData\Local\Temp\winslim-audit-native-serialized-start.png`.
- Arranque corregido (banner autocontenido, sin separador duplicado): `C:\Users\Administrador\AppData\Local\Temp\winslim-audit-final-start.png`.
- Rejilla problemática reproducida antes de la corrección: `C:\Users\Administrador\AppData\Local\Temp\winslim-audit-native-split.png`.

## Verificación

- `cargo test --manifest-path src-tauri/Cargo.toml --lib`: **608 passed, 0 failed**.
- `npm.cmd run e2e:build`: **completado**, `svelte-check` 0 errores/0 avisos, Vite correcto y ejecutable debug generado.
- `node --check tests/e2e/smoke.mjs`: correcto.
- `npm.cmd run test:frontend-logic`: correcto.
- `node scripts/verify-test-surface.mjs`: 236 contratos correctos.
- `node scripts/verify-build-scripts.mjs`: 120 comprobaciones correctas.
- `npm.cmd run check`: **completo**, incluyendo Clippy `-D warnings`, `cargo fmt --check`, 608 tests Rust, i18n (15 idiomas/855 claves), contratos, enlaces y fuentes de instalación.

El E2E WebDriver completo no se lanzó en esta máquina porque no existe `msedgedriver.exe` en `PATH`; la ventana nativa sí se ejecutó y se inspeccionó con capturas de pantalla. 

## Segunda pasada: transición real de rejilla y builds cruzadas

La interacción nativa 1 → 2 → 3 → 4 reprodujo un caso que la batería anterior
no cubría: la primera casilla podía conservar el viewport anterior mientras las
casillas nuevas ya mostraban el banner compacto. El log correlacionado registró
`resize` para las cuatro pestañas, pero el primer repintado no siempre se
reintentaba cuando la shell estaba ocupada. `TerminalPane.svelte` ahora marca
el cambio de rejilla antes del resize y programa un segundo intento sin cursor;
la captura `C:\Users\Administrador\AppData\Local\Temp\winslim-audit-pass-fixed-split4.png`
muestra las cuatro casillas homogéneas después del arreglo.

La build Linux en Ubuntu WSL terminó correctamente en **547 s** y produjo un
AppImage de **88 MB**. El smoke interno validó runtime, ventana, frontend, IPC,
xterm y PTY; `sha256sum -c` y `linux/validate-release.sh` también pasaron. Se
observaron solo avisos no fatales de linuxdeploy (`AppStream` ausente, `usr/lib64`
inexistente y `find` sin permiso sobre `lost+found`).

La build Linux descubrió además una regresión de flujo: `npm ci` en el checkout
compartido eliminaba el binding opcional `@tauri-apps/cli-win32-x64-msvc`.
`linux/build.sh` ahora aparta temporalmente el `node_modules` Windows, instala el
árbol Linux y lo restaura mediante `trap`, incluso si la build falla. La
verificación de scripts cubre este contrato.

Tras restaurar el binding Windows, `npm run e2e:build` volvió a producir el EXE
debug y la batería completa terminó con **121 comprobaciones de build, 236
contratos de tests, 608 tests Rust, svelte-check sin errores, fmt y Clippy sin
warnings**. En una repetición, Maven Central respondió con timeout de 5 s; el
checker lo trató como aviso externo y continuó, sin afectar a las validaciones
locales.

## Build Windows release y comprobación final

La build real `windows/build.ps1 -NoRun -NoExtendedTests -AllowOfflineChecks
-NonInteractive` terminó correctamente en **245,9 s**. En el entorno con red se
validaron 27 enlaces, 16 fuentes de instalación y 71 identificadores de WinGet;
el frontend produjo 144 módulos y `svelte-check` quedó en 0 errores/0 avisos.
Cargo compiló el ejecutable release x64 y la validación PE/runtime comprobó 37
artefactos. El smoke mínimo confirmó ventana, frontend, xterm, IPC y primera PTY.

La carpeta portable resultante contiene `winslim-terminal.exe`, ConPTY,
`OpenConsole.exe`, `WebView2Loader.dll` y 12 recursos integrados. El ZIP
`WinSlimTerminal-Unpacked-1.4.4.zip` pesa 4,2 MB y su SHA-256 se añadió al
manifiesto sin borrar el hash previo del AppImage Linux; ambos hashes coinciden
con `Get-FileHash` (2/2). Esta ejecución omitió deliberadamente el E2E WebDriver
ampliado (`-NoExtendedTests`), porque este host no tiene `msedgedriver.exe`.

## Revisión redundante de funciones, tiempos y logs

La batería `npm run check` se repitió completa fuera del sandbox y volvió a
terminar con código 0: 608 tests Rust, 121 contratos de build, 236 contratos de
tests, 30 comprobaciones lógicas, i18n completo, Clippy/fmt y `svelte-check`
limpios. La comprobación de hashes conserva y valida los dos artefactos.

Se revisó el log de la última sesión nativa (`c6x106`): 39 líneas, cero errores,
sin excepciones ni `ReferenceError`; en el resize de cuatro paneles aparecen
los cuatro `terminal.resize` y los cuatro `Repintado de banner solicitado` con
`paneCount=4`. Los `ReferenceError: paneCountChanged` que siguen apareciendo
en `main.log` pertenecen a la sesión histórica `c3yzxf`, anterior al arreglo,
no a la build actual.

Métricas observadas en el informe E2E pasado: interfaz lista en 2,985 ms,
primer banner tras terminal en 2,946 ms de media, cambio de shell en 591 ms,
`env_refresh` en 1,500 ms y `install_refresh` en 17,385 ms. Las fases más largas
son entorno/dependencias y rejilla/redimensionado porque incluyen esperas de
sondas, creación de pestañas y varias ventanas; las operaciones IPC individuales
se mantienen normalmente por debajo de 70 ms. No se encontró una llamada
bloqueante nueva ni un bucle de repintado infinito.

La revisión de estados pendientes descubrió y corrigió además el caso de dividir
una casilla mientras se edita una línea: el marcador de rejilla ya no expira a
los 900 ms ni se limpia al preparar el siguiente frame; solo desaparece cuando
`pty_refresh_banner` confirma el repintado. Se añadió una aserción de regresión y
`svelte-check` volvió a pasar con 0 errores y 0 avisos.

Después de ese ajuste se repitió `npm run e2e:build`: frontend de 144 módulos y
ejecutable debug generado en 48,09 s, sin errores de compilación. Dos sondas
externas (Chocolatey y Maven Central) agotaron el timeout de 5 s en esa pasada;
el flujo las dejó como avisos no bloqueantes y el resto de 14 fuentes respondió.

## Pasada final: wrapper, dependencias y fastfetch

`windows/build.bat` contenía una BOM UTF-8 (`EF BB BF`) delante de `@echo off`;
`cmd.exe` la mostraba como `´╗┐` y continuaba con código 1. Se eliminó la BOM y
`cmd /d /c windows\\build.bat -Help` termina ahora con código 0.

El panel Entorno y dependencias ya no muestra el botón redundante
«Actualizar/Actualizando»: la detección rápida inicial y la renovación en
segundo plano se conservan, pero no compiten con la inspección del usuario ni
con WebDriver. El smoke comprueba que el botón no vuelve a aparecer. También se
añadieron descripciones para Hyper-V, Virtual Machine Platform y Windows
Sandbox, y la prueba admite correctamente tarjetas de una sola acción (no solo
acordeones).

La última batería contra el release Tauri completo pasó todas las fases: **884
métricas, 63,3 s**, 1→2→3→4→1→4, matriz de tamaños con/sin explorador y cuatro
repeticiones de fastfetch. La supuesta «línea mezclada GPU» era un falso
positivo del detector: `GPU ... (1 GB)` es una línea válida. El patrón ahora
solo marca una GPU que invade otro campo. Se añadió además una barrera backend
que evita reenviar un banner idéntico cuando la shell ya lo pintó.

Captura visual final (release, banner completo y prompt separado):
`C:\\Users\\Administrador\\AppData\\Local\\Temp\\winslim-final-fastfetch-audit.png`.

## Auditoría específica de límites del fastfetch

La revisión final cubrió también los estados que una ventana normal no alcanza:
la generación de cada fila, el recorte de valores traducidos, la cuenta de filas
físicas después del reflujo xterm, el viewport aún no medido (`cols=0`) y las
transiciones con la pestaña desmontándose. Se corrigió que el separador y las
filas compactas pudieran superar el ancho real en casillas de 1–2 columnas; el
cálculo de limpieza usa ahora el mismo respaldo de 80 columnas que el generador.
Se añadieron pruebas para anchos 1–5 y para ANSI/continuaciones.

`TerminalPane.svelte` ya no deja callbacks de resize o repintado escribir sobre
un xterm destruido y reintenta un IPC transitorio sin perder la petición
pendiente. La build Tauri se regeneró después de estos cambios y el E2E de
release volvió a pasar: **893 métricas, cero errores en el log, 65,3 s**;
banner 1→2→3→4→1→4, matrices responsive con/sin explorador, cuatro tamaños de
fastfetch y entrada larga durante el repintado.

Pruebas de regresión locales: 40 tests de `system_info`, 10 de `terminal::tabs`,
frontend/contratos/superficie lógica, `svelte-check` 0/0, fmt y `git diff --check`.

## Auditoría de traducciones, preferencias de idioma y alias

La comprobación `scripts/verify-i18n.mjs` confirma la paridad exacta de los 15
catálogos (855 claves), 397 usos del frontend y 81 del backend, incluidos los
marcadores interpolados y las etiquetas dinámicas del banner. Se corrigió una
regresión de normalización: perfiles o culturas como `EN-us`, `es_ES.UTF-8`,
`PT-br` y `AUTO` ya se resuelven al catálogo base correcto en vez de caer
silenciosamente al español. El runner de `help` también genera su mensaje de
sección desconocida con el idioma activo en CMD, PowerShell y shells POSIX.

La auditoría de alias detectó dos colisiones reales de Windows que las pruebas
anteriores no modelaban: CMD y PowerShell no distinguen mayúsculas, por lo que
`Deploy.ps1`/`deploy.bat` se agrupan ahora antes de generar el inicializador;
además un script llamado `Clear.ps1` o `INSTALL.bat` ya no puede ocultar los
alias reservados. Bash y Fish conservan su semántica sensible a mayúsculas.
Se añadieron regresiones para ambos casos y para el mensaje localizado del
runner de ayuda.

Verificación posterior: **615 tests Rust**, Clippy `-D warnings`, i18n,
superficie lógica y `git diff --check` pasan sin errores.

## Dependencias y virtualización (última pasada)

Se retiraron del catálogo de Windows las tres acciones de VMware Workstation
(descarga, actualización y versión). No eran instalaciones automatizables:
solo abrían el portal de Broadcom y podían dar la impresión de que el producto
se instalaría desde la aplicación. También se eliminó su descripción estática
y se actualizaron los contratos E2E para exigir únicamente Hyper-V, Virtual
Machine Platform, Windows Sandbox, QEMU y VirtualBox.

Las acciones restantes mantienen sus sondas y descripciones: las capacidades
de Windows consultan el estado real de las características, QEMU/VirtualBox
comprueban rutas instaladas fuera de `PATH`, y el panel distingue una tarjeta
de acción única de un submenú. El catálogo sigue traduciendo etiquetas,
subgrupos y descripciones en la frontera con el frontend.

La comprobación integral `npm run check` se repitió con red habilitada: 26
enlaces, 16 fuentes de instalación y 71 identificadores WinGet respondieron;
Svelte no produjo errores ni avisos y la batería terminó con **615 tests Rust**
correctos. El único texto adicional del compilador fue el mensaje informativo
del linker MSVC al crear la biblioteca `.lib/.exp`; no es un fallo de build.

## Regresión reproducida en el E2E y corrección final

La build de Windows del 28 de agosto sí llegó a ejecutar el WebDriver, pero
falló al reducir una rejilla de cuatro paneles a 980x640 (43x14 celdas). El
informe `winslim-terminal-e2e-451cfe7bab0449cc8c5b712d7771df63.json` mostró dos
paneles empezando por la cola `2C/16T)` del banner anterior y dos con un banner
recortado. El log confirmó que el cursor de esos paneles todavía estaba dentro
de las filas que iban a limpiarse.

`refresh_banner` rechazaba correctamente escribir sobre el cursor, pero
devolvía `true`; el frontend interpretaba el rechazo como repintado completado
y perdía el reintento. Ahora devuelve `false` cuando aplaza el repintado,
manteniendo la petición pendiente hasta que ConPTY termina de dibujar el
prompt. No se aumentó el timeout del smoke ni se ocultó el fallo.

Tras recompilar `npm.cmd run e2e:build`, el E2E nativo con WebView2 y el
EdgeDriver compatible pasó todas las fases: **64,5 s, cero errores de log,
1→2→3→4→1→4, entrada larga durante resize, matriz responsive con y sin
explorador y cuatro repeticiones de fastfetch**. Las repinturas de la matriz
quedaron entre 93 y 271 ms y las repetidas entre 80 y 108 ms. El informe final
es `C:\Users\Administrador\AppData\Local\Temp\lterminal-smoke-e2e-7276-1787947005524.json`.

La prueba unitaria de regresión quedó además aislada en `terminal::tabs` y la
ejecución focalizada posterior pasó **11 tests**; la batería completa anterior
había pasado **615**.

## Auditoría visual reproducible (28 de agosto)

La ejecución no se dio por buena solo por las aserciones del DOM. Se
conservaron y revisaron capturas PNG de cada transición de rejilla y del punto
de fallo. En `e2e-4032`, `e2e-15484`, `e2e-10084` y `e2e-4704` la extracción de
texto de xterm contenía una sola cabecera por panel, pero la captura mostraba
capas antiguas de banners apiladas en la casilla superior izquierda después de
la matriz responsive. Esto demuestra un artefacto de renderizado canvas/WebView2
que el smoke anterior no podía detectar.

Se serializó la transición backend completa (estado + ANSI + emisión) y el
frontend ahora invalida y repinta el rango visible de xterm en dos frames,
limpiando también sus capas canvas. La limpieza agresiva para cualquier cambio
de viewport se descartó porque podía ocultar el prompt al activar una pestaña;
solo el cambio real de rejilla usa el borrado de superficie. Las capturas
`08-rejilla-4-estable` siguen homogéneas, mientras que los intentos posteriores
continúan fallando en la matriz o, de forma intermitente, en el aislamiento de
pestañas: no se marca esta auditoría como «verde» hasta que una ejecución
completa y su PNG final no presenten esos residuos.
